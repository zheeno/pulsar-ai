# Optional agent / desktop overrides (Tauri stores secrets in the OS keychain)

# Path to LangChain worker (defaults to packages/agent/dist/worker.js)
# NGX_AGENT_WORKER=/absolute/path/to/packages/agent/dist/worker.js
