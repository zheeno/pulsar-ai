# Host nginx — pulsar-api.antimony.com.ng (API)
# Install: sudo cp deploy/nginx/host/pulsar-api.conf /etc/nginx/sites-available/
#          sudo ln -sf /etc/nginx/sites-available/pulsar-api.conf /etc/nginx/sites-enabled/
server {
    listen 80;
    server_name pulsar-api.antimony.com.ng;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
