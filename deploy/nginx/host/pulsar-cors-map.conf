# Include from nginx http {} context (installed to /etc/nginx/conf.d/)
map $http_origin $pulsar_cors_origin {
    default "";
    "https://pulsar.antimony.com.ng" $http_origin;
    "http://pulsar.antimony.com.ng"  $http_origin;
}
